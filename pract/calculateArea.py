



# Write a Python program which accepts the radius of a circle from the user and compute the area.
        # r = 1.1
        # Area = 3.8013271108436504
from math import pi
radius = float(input("input the radius for circle: "))
area = pi * radius ** 2
print("Area of circle is %d" % (area))
